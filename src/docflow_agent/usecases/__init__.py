"""Usecase layer entrypoints.

This package contains application-facing orchestration functions.
Usecases coordinate `core`, `outbound`, and `workflow` without owning
long-lived execution state themselves.
"""
